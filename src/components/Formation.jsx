import React from 'react';
import { Box, Typography } from '@mui/material';

function Formation() {
    return (
        <Box sx={{ p: 3 }}>
           <Typography sx={{ marginBottom: 1.4 }} variant="h6">Formação</Typography>
           <Typography>Ciência da Computação</Typography>
        </Box>
    );
}

export default Formation;